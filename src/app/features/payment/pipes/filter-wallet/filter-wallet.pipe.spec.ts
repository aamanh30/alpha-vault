import { FilterWalletPipe } from './filter-wallet.pipe';

describe('FilterWalletPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWalletPipe();
    expect(pipe).toBeTruthy();
  });
});
