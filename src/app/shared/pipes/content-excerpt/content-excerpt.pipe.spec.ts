import { ContentExcerptPipe } from './content-excerpt.pipe';

describe('ContentExcerptPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentExcerptPipe();
    expect(pipe).toBeTruthy();
  });
});
