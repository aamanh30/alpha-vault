import { LoadingLayerDirective } from './loading-layer.directive';

describe('LoadingLayerDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingLayerDirective();
    expect(directive).toBeTruthy();
  });
});
